name: Security

on:
  push:
    branches: [main, master, "security/**"]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  deps-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install
        run: bun install
      - name: Dependency audit
        run: bun audit
        continue-on-error: true

  semgrep:
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@v4
      - run: semgrep scan --config p/typescript --config p/javascript --config p/sql --error
